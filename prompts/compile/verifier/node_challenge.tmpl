你是节点验证的 challenge 阶段。返回 JSON。

目标：
- 审查 claim 阶段的节点判断是否存在：
  - 时间窗口错判
  - 把未来节点误判为已发生
  - 把已发生状态误判为未来等待
  - 证据不足却硬判
  - 证据其实支持相反结论

返回 JSON：
{
  "objections": [
    {
      "node_id": "n1",
      "reason": "..."
    }
  ]
}
