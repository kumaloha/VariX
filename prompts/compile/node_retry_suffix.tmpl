The previous node extraction output failed validation. Redo it and follow these rules:
- output exactly one valid JSON object, no markdown code fence, no commentary
- include top-level keys: `graph`, `details`, `topics`, `confidence`
- produce at least {{.MinNodes}} nodes
- output nodes only; do not output any edges
- every node must include `form` and `function`
- use only `form` values `observation`, `condition`, `judgment`, `forecast`
- use only `function` values `support`, `transmission`, `claim`
- prefer over-splitting to under-splitting
- split mixed fact / judgment / prediction statements into separate nodes when possible
- if a sentence contains a multi-step causal chain, split it into multiple nodes instead of one fat node
- if a sentence contains because / therefore / so / which means / implying / driven by / due to / despite / as a result, split the linked ideas into separate nodes
- if a sentence contains coordinated claims such as A and B or not X and not Y, split them unless they are the same proposition restated
- extract transmission nodes separately from observed flow/positioning evidence when the article distinguishes them
- for flow/positioning articles, split support observations, transmission mechanisms, and judgment/forecast claims into separate nodes
- if evidence nodes support a claim but the bridge pricing/allocation rule is missing, add a transmission node for that bridge
- do not leave evidence attached directly to claim when an implicit pricing/positioning mechanism is needed to connect them
- if one force dominates another in pricing, positioning, or allocation, prefer a transmission node over a judgment node
- give every current-frame implicit condition an `occurred_at` timestamp; use the article frame timestamp if needed
- prefer mechanism nodes over slogan nodes when the mechanism explains continued allocation or positioning
- use RFC3339 timestamps for populated time fields
- make `details` an object and place notes under `details.caveats`
