The previous output failed validation. Redo it and follow these rules:
- output exactly one valid JSON object, no markdown code fence, no commentary
- include all required top-level keys: `summary`, `drivers`, `targets`, `details`, `topics`, `confidence`
- normalize to article-level `driver -> target` using the provided causal projection
- make `drivers` concrete mechanisms
- make `targets` market changes, not bare asset nouns
- do not leave `drivers` or `targets` empty if the causal projection contains a market thesis
- do not output graph nodes or graph edges in this stage
