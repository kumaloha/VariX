Attach evidence links inside the following node set.
Each line is:
node_id | node_text | role=<role> | ontology=<ontology> | quote=<source_quote>

{{.Nodes}}

Full article (for discourse context only; do not invent new nodes):
{{.Article}}
