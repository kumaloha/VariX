The previous graph output failed validation. Redo it and follow these rules:
- output exactly one valid JSON object, no markdown code fence, no commentary
- include top-level keys: `graph`, `details`, `topics`, `confidence`
- output edges only; do not output any nodes
- produce at least {{.MinEdges}} edges
- use only edge kinds `drives`, `substantiates`, `gates`, `explains`
- every edge must use exactly these keys: `from`, `to`, `kind`
- do not use alternate keys such as `source`, `target`, or `relation`
- if a source node is `function=transmission`, default to `drives`; if a source node is `function=support`, default to `substantiates`
- if A changes B in the world, use `drives`; if A only supports or proves B, use `substantiates`
- use `gates` only when the source node is a condition node
- use `explains` when a node mainly provides the interpretive frame or theory for another node
- for flow/positioning articles, keep support -> claim evidential and transmission -> claim causal when the mechanism is the bridge
- ensure the dominant market thesis has at least one causal path built from `drives` edges
