Payload:
{{.PayloadJSON}}

Generated draft:
{{.GeneratedJSON}}

Challenge corrections:
{{.ChallengedJSON}}

Return the final complete compile package.
Output requirements:
- return raw JSON only
- do not wrap in markdown fences
- do not add prose before or after the JSON
- emit exactly one complete JSON object and nothing else
