The previous node-challenge output failed validation. Redo it and follow these rules:
- output exactly one valid JSON object, no markdown code fence, no commentary
- include top-level keys: `graph`, `details`, `topics`, `confidence`
- output additional nodes only; do not repeat unchanged existing nodes
- output nodes only; do not output any edges
- every additional node must include `form` and `function`
- add finer-grained nodes when an existing node still mixes evidence, mechanism, judgment, prediction, or coordinated claims
- add the missing bridge transmission node when evidence nodes and judgment nodes are present but the transmission connecting them is missing
- for flow/positioning articles, add the missing transmission bridge when support observations and claim nodes are present without it
- add a transmission node when a pricing rule, allocation rule, or positioning rule is needed to connect evidence to claim
- prefer transmission nodes over judgment nodes for pricing-dominance or allocation-preference bridges
- make `details` an object and place notes under `details.caveats`
