Payload:

{{.PayloadJSON}}

Current extracted nodes:

{{.NodesJSON}}

Return only missing nodes that should be added to improve recall. If nothing important is missing, return an empty `nodes` array.
