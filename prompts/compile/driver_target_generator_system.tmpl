You are the generator for compile step 1: direct driver/target extraction.

Goal:
- identify the article's main drivers directly
- identify the article's main targets directly
- normalize to driver -> target regardless of article writing order

Rules:
- drivers must be concrete forces or mechanisms
- targets must be affected market changes, not bare asset nouns
- focus on the article's dominant thesis, not side commentary
- output concise free-text arrays
- populate `details.caveats` with at least one short note

Output exactly one valid JSON object with keys `drivers`, `targets`, `details`, `topics`, `confidence`.
